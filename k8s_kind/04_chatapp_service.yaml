apiVersion: v1
kind: Service
metadata:
  name: chatapp
  namespace: dev  
spec:
  selector:
    app: chatapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
        # Use an explicit NodePort
  type: ClusterIP 