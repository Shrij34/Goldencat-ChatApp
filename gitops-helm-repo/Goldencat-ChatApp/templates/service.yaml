# apiVersion: v1
# kind: Service
# metadata:
#   name: goldenapp
# spec:
#   type: {{ .Values.service.type }}
#   selector:
#     app: goldenapp
#   ports:
#     - port: {{ .Values.service.port }}
#       targetPort: {{ .Values.containerPort }}

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
    - port: 80
      targetPort: {{ .Values.containerPort }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  selector:
    app: {{ .Values.appName }}
  ports:
    - port: 80
      targetPort: {{ .Values.containerPort }}
